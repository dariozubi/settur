'use client'

import { Trigger, Portal, Root, Close } from '@radix-ui/react-dialog'

const Dialog = Root
export const DialogTrigger = Trigger
export const DialogPortal = Portal
export const DialogClose = Close

export default Dialog
